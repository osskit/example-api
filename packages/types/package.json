{
  "name": "@osskit/service-types",
  "version": "0.0.1",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/osskit/service-example.git"
  },
  "scripts": {
    "build": "rm -rf dist && rm -rf generated && mkdir generated && cp -r $PWD/../../src/schemas/models.ts ./generated/models.ts && tsc",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.23.5"
  }
}
